import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stardiary.app',
  appName: '星空日记',
  webDir: 'dist',
  android: { backgroundColor: '#060618' },
};

export default config;
